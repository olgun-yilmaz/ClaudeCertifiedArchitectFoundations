## Q1
Correct: B
Explanation: A skill's SKILL.md only loads on demand when explicitly invoked, so it is never consulted during a routine deploy_service call; argument-formatting facts like accepted values and conditional requirements belong in the tool's own description/input schema, which Claude reads on every call it considers making.
Why-A: SKILL.md would fix the runbook documentation problem but is invisible during a normal deploy_service call since skills only load when invoked (e.g., via /deploy-runbook) — the argument rule would still not be seen on the vast majority of deploys.
Why-B: Correct — the tool's own description/schema is read every time Claude considers calling that tool, unlike a skill that loads only on demand; this is the one place that guarantees the rule is seen on every call.
Why-C: Root CLAUDE.md is always loaded and would technically be seen, but it is the wrong mechanism for tool-argument formatting — it bloats every session's context with tool-specific schema detail even when no deploy is happening, instead of binding the rule directly to the tool the way a description does.
Why-D: context: fork governs how an invoked skill's own exploration is isolated from the main session; it does not make a skill run automatically before every unrelated tool call, so this doesn't solve the visibility problem either.
Source: Lesson 2.1 — Designing Tool Interfaces
Source: Lesson 3.2 — Custom Slash Commands & Skills

## Q2
Correct: A
Explanation: A response can contain explanatory text and a queued tool_use block together, so any text-content check (a marker phrase, its position, or patching around it) is unreliable; only stop_reason reliably distinguishes "still has a tool call to run" (tool_use) from genuinely done (end_turn), which is also what guarantees every executed tool call gets its audit entry.
Why-A: Correct — stop_reason: "tool_use" means keep looping and execute (and log) the queued call regardless of any text elsewhere in the same response; only stop_reason: "end_turn" means truly done.
Why-B: Requiring the marker to trail the response is still a text-content heuristic, not a documented completion signal, and a model could still produce a trailing marker with a tool call queued right before it in the same turn.
Why-C: Backfilling a synthetic audit entry patches the symptom (a missing log line) without fixing the underlying problem — the process_refund call itself was never actually executed by the harness, so the customer's dispute is still unresolved even with a fabricated log entry.
Why-D: A PostToolUse hook only fires after a tool has executed; if the harness already ended the turn on the marker, process_refund never ran, so there is no tool result for a PostToolUse hook to act on — this doesn't reach the actual failure point.
Source: Lesson 1.1 — Designing Agentic Loops

## Q3
Correct: B
Explanation: The fix should be proportionate to how much trust each category has actually lost — an 80% false-positive rate has already broken trust and warrants disabling the category while its criteria are rewritten, while a 25% rate that reviewers still find useful should stay active with incremental criteria tightening rather than being held to the same drastic response.
Why-A: Treating both categories identically ignores that the performance category is still providing real value to reviewers; disabling it too destroys working functionality that didn't need drastic action.
Why-B: Correct — the response should scale with actual damage to trust: disable-and-rewrite for the category that's already been tuned out (security), incremental tightening for the category still being used productively (performance).
Why-C: Raising a self-reported confidence threshold doesn't fix vague criteria and confidence remains poorly calibrated; this repeats the guide's documented anti-pattern regardless of which category it's applied to.
Why-D: Leaving the already-untrusted security category active with only a disclaimer doesn't restore any trust in it and doesn't fix the vague criteria producing 80% false positives — it just labels the existing problem instead of fixing it.
Source: Lesson 4.1 — Prompts with Explicit Criteria

## Q4
Correct: B
Explanation: A "case facts" implementation that regenerates a free-text summary on every pass is still fully exposed to the same lossy summarization process it's meant to protect against; true case facts must be structured, specific fields populated once from source data and carried forward untouched by any later summarization pass.
Why-A: Reducing how often summarization runs delays the loss but doesn't prevent it — the free-text field is still regenerated by the same lossy process every time it does run, and disputes can easily outlast a lower summarization frequency.
Why-B: Correct — structured, named fields (refund_amount, invoice_id, promised_delivery_date) excluded entirely from summarization, populated once and carried forward verbatim, are what actually survive repeated summarization passes.
Why-C: Instructing the summarizer to "never drop these numbers" is still a prose instruction to the same probabilistic process that's already losing them — the guide's fix is a structural exclusion, not a stronger reminder to the same mechanism.
Why-D: Re-deriving the figures from raw tool results at every compaction is unnecessary rework and becomes unreliable if those same raw tool results have themselves already been trimmed or summarized by that point — a case-facts field set once from source data doesn't have this dependency.
Source: Lesson 5.1 — Managing Conversation Context

## Q5
Correct: C
Explanation: Letting the agent grant itself an emergency override defeats the deterministic guarantee the hook exists to provide, since it reintroduces the agent's own judgment as the decision point; the correct fix is an expedited human-confirmation path (a fast, pre-authorized approver), not a bypass the agent can invoke on its own.
Why-A: An override string the agent includes in its own command is still the agent deciding when the safeguard doesn't apply to it — an audit trail records that it happened, but doesn't prevent it, which was the entire point of the hook.
Why-B: Removing the hook during business hours on a time-based heuristic abandons the deterministic guarantee entirely and for a reason ("usually safe") that doesn't hold in every case, which is exactly what "must never without sign-off" rules out.
Why-C: Correct — the guarantee must stay with a human decision-maker; making that human step fast (a pre-authorized on-call approver confirming in seconds) preserves the deterministic guarantee while addressing the genuine speed concern.
Why-D: Logging every use of the override after the fact documents that the guardrail was bypassed but does not prevent the bypass — it's an audit trail on top of a broken guarantee, not a fix to the guarantee itself.
Source: Lesson 1.4 — Workflow Enforcement & Handoff

## Q6
Correct: D
Explanation: CLAUDE.md files at every level concatenate into context rather than one overriding another by directory depth, so the directory file's "may apply directly" instruction doesn't silently win over root's dry-run requirement — both are loaded together as a live contradiction that must be resolved directly, alongside separately correcting the now-stale "disposable" claim.
Why-A: This accepts a precedence model that isn't how CLAUDE.md works; even after fixing the outdated language, the underlying contradiction-handling mechanism (concatenation, not override) would still be misunderstood the next time a similar conflict arises.
Why-B: There's no such Bash-vs-file-operation split in how CLAUDE.md applies; directory-level files load into context regardless of which tool a subsequent command uses.
Why-C: This reaches a safety-conscious instinct but invents a special-cased absolute rule that isn't how the mechanism actually works — CLAUDE.md doesn't hard-code exceptions for certain instruction types; it concatenates everything, and the contradiction has to be resolved in the instructions themselves.
Why-D: Correct — concatenation, not override, is the actual mechanism; the team needs to remove the live contradiction directly, fix the outdated "disposable" premise now that the sandbox is networked, and confirm what's actually loaded with /memory.
Source: Lesson 3.1 — CLAUDE.md Hierarchy & Scoping

## Q7
Correct: C
Explanation: Two different failure causes (transient replica lag vs. a genuine reconciliation mismatch) need different agent behavior (retry vs. never retry, escalate) and two different audiences (an internal audit log needing the exact cause, a customer needing only the approved generic message) — a single error string or a single boolean collapses distinctions the agent and the compliance process both need.
Why-A: The category and retryAfter hint settle the retry decision, but reusing the existing error text unchanged for both audiences leaves the exact technical cause out of the audit log and keeps sending the customer the text policy prohibits.
Why-B: A single isRetryable flag defaulting to true would have the agent retry the reconciliation-mismatch case too, which can never succeed on retry and shouldn't be retried at all.
Why-C: Correct — categorizing the failure, deriving retryability from that category, and providing both a technical (audit) message and a separate user-facing message satisfies the retry-logic need and both audiences' requirements at once.
Why-D: Pushing a raw replica ID and sync timestamps back to the agent asks it to infer the category from infrastructure detail it has no reliable way to interpret, and still leaves the audit log and the customer with the same unusable error text.
Source: Lesson 2.2 — Structured Error Responses

## Q8
Correct: A
Explanation: Partial, verified progress should be reported honestly with a structured note about what wasn't attempted and why, so the coordinator — not the subagent acting alone — can weigh the deadline against the coverage gap; silently succeeding, silently retrying without any status update, and discarding good partial work are all documented failure patterns.
Why-A: Correct — reporting the five verified sources plus a structured note on the rate-limited gap gives the coordinator exactly what it needs to make a deadline-aware call, without hiding or discarding anything.
Why-B: Reporting total failure discards five sources that were fully verified and usable, an overcorrection that wastes real, completed work under exactly the deadline pressure that makes it most valuable.
Why-C: Marking the result "complete" when three of eight planned sources were never attempted is the silent-false-success anti-pattern — the coordinator has no way to know the report's source coverage is narrower than planned.
Why-D: Retrying locally for a transient rate limit is reasonable, but doing so in total silence during a one-hour deadline denies the coordinator any visibility into progress when it most needs to decide whether to accept partial coverage or wait.
Source: Lesson 5.3 — Error Propagation in Multi-Agent Systems

## Q9
Correct: D
Explanation: The choice between plan mode and direct execution turns on ambiguity and scope, not on how much time is left; a 60-file fix with three genuinely different valid approaches is exactly the case plan mode exists for, and skipping that step under deadline pressure risks a wrong approach whose rework costs more time than planning would have — if the window can't absorb it, deferring the fix is safer than rushing it.
Why-A: Abandoning plan mode for a genuinely ambiguous, multi-file change purely because of a deadline inverts the guide's criterion (ambiguity/scope, not time pressure) and raises the odds of a costly wrong-approach rewrite.
Why-B: Approving a plan mode proposal without reading it defeats the entire purpose of plan mode, which is to actually surface trade-offs for a human to weigh before code changes; this isn't a real shortcut.
Why-C: Picking the most "conservative" of three approaches without evaluating trade-offs is still a decision under the same ambiguity plan mode exists to resolve, and "conservative" isn't a documented guarantee of correctness for a shared library used by 60 files.
Why-D: Correct — this matches the guide's criterion exactly: the multi-approach, wide-blast-radius fix calls for plan mode regardless of deadline, and if time truly doesn't allow it, deferring beats rushing an ambiguous change through direct execution.
Source: Lesson 3.4 — Plan Mode vs Direct Execution

## Q10
Correct: A
Explanation: PreToolUse fires before a tool executes, so at that point in the lifecycle there is no lookup_order result yet to rewrite — the hook can gate the outgoing process_refund call, but normalizing a different tool's returned data has to happen in a PostToolUse hook attached to that tool, run after it returns and before Claude reasons over the result.
Why-A: Correct — this is the precise lifecycle gap: PreToolUse never sees a tool's output, so the date-normalization half of the plan needs its own PostToolUse hook on lookup_order.
Why-B: Hooks are tied to specific lifecycle events (before or after tool execution); "running both before and after" isn't how a single PreToolUse registration works, and doesn't change what data is available at the pre-execution point.
Why-C: A single hook script can absolutely branch its logic by tool name; that isn't the actual problem here — the problem is which lifecycle stage the date-normalization logic needs to run at, not whether one script can distinguish tools.
Why-D: Moving the $500 refund block to PostToolUse would mean checking the amount only after process_refund has already executed, which is too late to block an over-limit refund from happening in the first place.
Source: Lesson 1.5 — Agent SDK Hooks

## Q11
Correct: D
Explanation: The fabrication is caused by rootCauseCategory being both required and restricted to one specific value with no way to represent a genuinely cross-cutting finding; adding a nullable option or an explicit "unclear"/"cross-cutting" enum value lets the model answer accurately while the schema — and the tool-call guarantee from tool_choice: "any" — stay exactly as they are.
Why-A: tool_choice: "any" guarantees some tool call happens at all; it has nothing to do with which values a required field inside that call must take, and reverting it would reopen the separate, already-solved problem of guaranteeing a tool call in the first place.
Why-B: Downgrading rootCauseCategory to optional free text throws away the structured triage bucket downstream tooling explicitly needs, overcorrecting rather than simply giving the model an accurate value it can select.
Why-C: Re-prompting the model to "pick a real category" pushes it right back toward fabricating a specific value for a finding that genuinely doesn't have one — the same pressure that caused the problem in the first place.
Why-D: Correct — a nullable or "cross-cutting"/"unclear" enum value gives the model an accurate way to represent findings that don't fit one bucket, without touching the tool-call guarantee or removing the field's value to downstream triage.
Source: Lesson 4.3 — Structured Output with Tool Use

## Q12
Correct: B
Explanation: Correct secret handling (env-var expansion) and correct scoping (shared, committed .mcp.json) are configuration hygiene practices from a different lesson than the production-infra review/reversibility policy; satisfying the former says nothing about the latter, and a tool that can directly apply changes to a production cluster still needs its own deterministic gate before it executes.
Why-A: This conflates two separate requirements — secret handling and team-wide scoping are necessary but don't by themselves satisfy an unrelated policy requiring every prod-infra change to be reviewable and reversible.
Why-B: Correct — the MCP configuration is done correctly for its own concerns, but the review/reversibility policy is a separate requirement that needs its own deterministic mechanism (a hook, approval step, or dry-run gate) on the mutating tool itself.
Why-C: Moving the token to personal configs breaks the correctly-working shared-scope pattern for a reason unrelated to the actual gap (the missing review gate), and personal-scope secrets aren't inherently safer here.
Why-D: A prompt-level example is a probabilistic nudge, not the deterministic guarantee a "must be reviewable and reversible" production policy requires.
Source: Lesson 2.4 — Integrating MCP Servers

## Q13
Correct: C
Explanation: When two credible sources disagree because they measured different populations rather than because one is wrong, the correct handling is to preserve both figures with their attribution and methodology and let that difference explain the gap — not to pick the more flattering number, average incompatible measurements, or discard everything.
Why-A: Picking the more impressive figure for the client's narrative is exactly the "choose the more favorable one arbitrarily" anti-pattern, made worse here since the actual driver of the gap (different sampled populations) is glossed over entirely.
Why-B: Averaging two figures measured against different populations manufactures a number neither source actually reported and obscures the real explanation for the difference.
Why-C: Correct — preserving both figures with attribution and their differing methodologies (10-50 vs. 1-200 employees) is exactly the guide's approach to conflicting-but-credible sources, and surfaces the actual likely explanation for the gap.
Why-D: Discarding both figures throws away genuinely useful, population-specific data instead of contextualizing it, when the real fix is explaining the difference rather than erasing it.
Source: Lesson 5.6 — Information Provenance & Multi-Source Synthesis

## Q14
Correct: A
Explanation: Bypassing the coordinator for a "just data" handoff removes the coordinator's aggregation step entirely, so the document-analysis subagent's reliability assessment never gets merged with the web-search subagent's raw list before the report subagent uses it — the fix is routing every inter-subagent handoff back through the coordinator, which aggregates and vets before delegating onward.
Why-A: Correct — routing the handoff back through the coordinator restores the aggregation step that merges the reliability assessment with the raw source list before either reaches the report subagent, and restores the coordinator's visibility into what gets delegated.
Why-B: Letting the report subagent read both other subagents' raw outputs directly still bypasses the coordinator's aggregation and observability; a self-reported log of sources used doesn't recover the reliability assessment that never got merged in.
Why-C: Removing unreliable citations after they're already used in a drafted report treats the symptom after the fact; it doesn't prevent them from being cited if the citation-checking subagent misses one, and adds another component rather than fixing the actual handoff.
Why-D: Having the report subagent call the document-analysis subagent directly reintroduces the same subagent-to-subagent communication anti-pattern in a different form, still bypassing the coordinator's aggregation and observability.
Source: Lesson 1.2 — Multi-Agent Orchestration

## Q15
Correct: B
Explanation: A naive restart-with-the-same-prompt retry gives the model no new information about what was actually wrong; the fix is retrying with the original function, the failed stub, and the specific observed runtime mismatch as concrete feedback, combined with a low, fixed retry ceiling and a human-review fallback so the loop can't keep consuming the fixed CI budget indefinitely.
Why-A: More retries of the same blind, feedback-free prompt just spends more of the fixed CI budget on the same 8% without giving the model anything new to correct against.
Why-B: Correct — concrete feedback (the actual expected-vs-actual mismatch) lets the model self-correct instead of guessing again, and a low retry ceiling with a human-review fallback bounds the time cost within the fixed budget.
Why-C: Plan mode resolves upfront ambiguity about approach; a wrong runtime assertion value is a testable, executional error already caught by running the test, which plan mode's proposal step doesn't address.
Why-D: The scenario already runs the generated test against the real function to detect the mismatch — this option describes what's already happening as if it were a new fix, without changing anything about how the retry loop responds to a failure.
Source: Lesson 4.4 — Validation & Retry Loops

## Q16
Correct: C
Explanation: Path-specific rules should be scoped to the actual policy they govern, not merely grouped by shared file extension; a mandatory down-migration is a narrower, migration-specific requirement that shouldn't fire on every *.sql file (including ad hoc analysis queries), so it belongs in its own rule scoped to **/migrations/*.sql rather than folded into the unrelated sql-style.md.
Why-A: Consolidating by file extension alone ignores that the down-migration requirement should only ever apply inside migrations/, not to every *.sql file the style rule already covers — merging them would make the down-migration requirement fire where it shouldn't.
Why-B: Broadening the glob to include Python runner scripts adds unrelated scope creep to a rule-file question and doesn't address whether the down-migration requirement belongs in the style file at all.
Why-C: Correct — a new rule scoped specifically to **/migrations/*.sql keeps the down-migration requirement from firing outside migrations/, while the existing sql-style.md keeps governing formatting for every *.sql file as intended.
Why-D: This is a rule-placement question, not a technical-enforcement question — nothing in the scenario asks whether a commit should be blocked; treating a documented convention as if it required a PreToolUse hook solves a different problem than the one asked.
Source: Lesson 3.3 — Path-Specific Rules

## Q17
Correct: D
Explanation: Forcing the same named tool on every first turn is deterministic but blind to context — it re-verifies sources that a resumed session already has cached and verified, wasting a turn; the better approach keeps the forced tool_choice only for sessions/turns that lack a cached verification, and relaxes to "auto" otherwise.
Why-A: There is a real drawback: unconditionally re-running verify_source_credibility on already-verified, cached sources wastes a turn and contradicts the team's separate expectation of reusing prior sessions' work rather than re-researching from scratch.
Why-B: "any" only guarantees that some tool is called, not specifically verify_source_credibility, so it doesn't guarantee the compliance requirement (that this specific check has run) the way a forced named tool does.
Why-C: Reverting to a prompt-only instruction for a "must always verify before citing" requirement is the deterministic-enforcement anti-pattern — a probabilistic instruction is exactly what forced tool_choice was correctly replacing.
Why-D: Correct — conditioning the forced tool_choice on whether a cached verification already exists preserves the deterministic guarantee where it's needed while eliminating the wasted, redundant re-verification on resumed sessions.
Source: Lesson 2.3 — Tool Distribution & Tool Choice

## Q18
Correct: A
Explanation: fork_session branches from the resumed session's current conversation state, so it carries forward the exact same pre-merge utils signatures the engineer is trying to escape; the reliable fix is a fresh session given a structured summary of the refactor's plan and progress, explicitly told which two signatures changed so it re-reads exactly those.
Why-A: Correct — this matches the documented stale-context fix precisely: fork_session inherits the stale state, so a fresh session with a targeted summary of what changed is the actual fix.
Why-B: Forking is not a clean re-read of the current file state; it copies the conversation's existing state, stale signatures included, so it doesn't avoid working against them.
Why-C: Hoping the model "naturally notices" a signature mismatch relies on passive luck rather than a reliable mechanism, and doesn't address the stale context actively driving its reasoning.
Why-D: /compact summarizes conversation history to save space; it doesn't refresh or correct the substance of a stale cached fact, so the forked session would still start from the same outdated signatures, just described more concisely.
Source: Lesson 1.7 — Session State, Resumption & Forking

## Q19
Correct: C
Explanation: --output-format json paired with --json-schema makes Claude's own response schema-conformant JSON directly, removing the fragile freeform-text-plus-regex parsing step entirely, while -p keeps the run non-interactive — together satisfying both the non-interactivity and reliable-parsing requirements within the fixed budget.
Why-A: --output-format stream-json streams partial output incrementally, which helps with progress/timeout visibility but doesn't make the content itself conform to a severity schema — the regex-parsing reliability problem remains.
Why-B: Chaining a second Claude call to reformat the first call's freeform output adds latency and cost inside an already fixed, tight step budget, and introduces a new place for reformatting errors, when the first call could simply produce structured output directly.
Why-C: Correct — this is the documented mechanism for CI: -p for non-interactive execution, plus --output-format json and --json-schema so the findings (including severity) come back schema-conformant without any separate parser.
Why-D: Few-shot examples might reduce misparses somewhat but leave the fundamentally fragile freeform-text-plus-regex approach in place, rather than switching to a schema that guarantees the structure.
Source: Lesson 3.6 — Integrating Claude Code into CI/CD

## Q20
Correct: D
Explanation: The Batch API doesn't support multi-turn, result-dependent tool calling, so the genuinely agentic subset of work (PRs needing a ticket-status check with a possible follow-up call) should be split out and run through the synchronous API, while the bulk of the audit stays on Batch for its cost and latency-tolerance benefits.
Why-A: Adding the check as "more instructions" in the same batched request doesn't change the underlying constraint that a single batched request can't support a tool call whose follow-up depends on that call's result.
Why-B: Moving the entire 1,000-PR audit to synchronous abandons the cost and latency-tolerance benefits for the vast majority of PRs that don't need the new multi-turn check at all.
Why-C: Pre-fetching every referenced ticket's status for all 1,000 PRs regardless of relevance defeats the cost savings that were the reason to use Batch in the first place, and still can't represent a genuine follow-up call whose need depends on the first call's result.
Why-D: Correct — splitting the workload keeps the bulk of the audit on Batch for its cost/latency-tolerance benefits, while routing only the subset needing multi-turn, result-dependent tool calling through the synchronous API where that's supported.
Source: Lesson 4.5 — Batch Processing Strategies
